import { css } from "@emotion/react";

const homestyle = css({
  marginTop: 60,
  marginBottom: 60,
  padding: "10px 10px",
  minHeight: "calc(100% - 60px)",
  minWidth: "100vw",
  backgroundColor: "#e2f4f9",
});

export default homestyle;
